export class Post {
    constructor({intro, tags, title, previewImg, slug, pageEn, pagesVi}) {
        this.intro = intro;
        this.tags = tags;
        this.title = title;
        this.previewImg = previewImg;
        this.slug = slug;
        this.pageEn = pageEn;
        this.pagesVi = pagesVi;
    }
}